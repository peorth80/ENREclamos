# Pulumi

## Stacks
Solo esta el stack de DEV, cambia los valores en `pulumi.dev.yaml` con tus valores

Para agregar un nuevo stack (por ej. PROD), correr `pulumi stack init`, setear la info, y despues hacer `pulumi up --stack prod`